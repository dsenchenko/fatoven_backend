NODE_ENV=development
# Host port when running API on the machine (mdw-api uses 3000)
PORT=3001
# Host port for fatoven-postgres (mdw-db uses 5432)
DATABASE_URL=postgresql://fatoven:fatoven@localhost:5433/fatoven?schema=public
JWT_SECRET=dev-secret-change-in-production
JWT_EXPIRES_IN=7d
