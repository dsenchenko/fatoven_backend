# Copy to .env on the prod server before:
# docker compose -f docker-compose.prod.yml up -d --build

POSTGRES_PASSWORD=change-me-strong-password
JWT_SECRET=change-me-run-openssl-rand-base64-32
JWT_EXPIRES_IN=7d
